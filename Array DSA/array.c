#include<stdio.h>
#include<stlib.h>
Struct array
{
int *A;
int size;
int length;
};
int main(){
struct array arr;
printf("enter the size");
scanf("%d",&arr.size);
arr=A(int*)malloc(arr.size*size of (int));
arr.length = 0;
}
